# SPA for "504 Absolutely Essential Words" book

Navigate to the root dir, type `yarn` in order to install all dependencies.

Once installed hit `yarn start` - enjoy

To build a production version type `yarn build`